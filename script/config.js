export let config = {
    pipe: [], // position of the bird
    gap: 150, // px, gap between pipeUp & pipeDown

    // bird starting coordinates
    xPos : 10,
    yPos : 150,

    grav : 2.5, // OY coord. changing
    score : 0,
    step : 2, // OX coord. changing

    SPAWN_POINT_X : 100, // barrier spawn
    SCORE_POINT_X : 2, // score increase
};
